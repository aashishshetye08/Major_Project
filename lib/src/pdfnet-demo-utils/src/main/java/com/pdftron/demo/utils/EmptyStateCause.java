package com.pdftron.demo.utils;

/**
 * Keeps track of empty state in FileBrowserViewFragment
 */
public enum EmptyStateCause {
    NONE,
    SEARCH_RESULTS,
    FILTER_TYPE,
    FILTER_TYPE_IN_SEARCH_RESULTS
}
